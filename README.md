# CoastRP license list

`servers.txt` is the allowlist read by the CoastRP FiveM resources
(`coastrp-adminmenu`, `coastrp-sandeliai`) every 5 minutes.

- Add a server: add a line `<ip> adminmenu sandeliai` and commit.
- Revoke a server: delete its line and commit. It locks within 5 minutes.